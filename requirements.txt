python-dotenv>=1.0.0
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-docx>=1.1.0
python-multipart
# MCP research-tools server (mcp_server/scraper_server.py) + client
mcp>=1.2.0
httpx>=0.27.0
trafilatura>=1.12.0
ddgs>=9.0.0
pypdf>=5.0.0

# Used by providers/azure_provider.py (AsyncOpenAI client against the Azure
# OpenAI / Azure AI Foundry endpoint) and providers/mistral_provider.py
# (AsyncOpenAI client against Mistral's OpenAI-compatible chat completions
# endpoint).
openai>=1.50.0

google-genai>=1.0.0

# Persistent storage for generated reports (Postgres index + Storage bucket),
# used by services/storage_service.py when SUPABASE_URL is configured.
supabase>=2.0.0
