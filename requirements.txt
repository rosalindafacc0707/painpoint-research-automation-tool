anthropic>=0.40.0
python-dotenv>=1.0.0
fastapi==0.115.5
uvicorn[standard]==0.32.1

# MCP research-tools server (mcp_server/scraper_server.py) + client
mcp>=1.2.0
httpx>=0.27.0
trafilatura>=1.12.0
ddgs>=9.0.0

# Azure OpenAI / Azure AI Foundry provider switch (PROVIDER=azure in
# .env.development) — talks to your Azure OpenAI deployment via the
# AsyncAzureOpenAI client.
openai>=1.50.0
