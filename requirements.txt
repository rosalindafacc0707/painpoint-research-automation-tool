anthropic>=0.40.0
python-dotenv>=1.0.0

# MCP research-tools server (mcp_server/scraper_server.py) + client
mcp>=1.2.0
httpx>=0.27.0
trafilatura>=1.12.0
ddgs>=9.0.0

# Open-source / self-hosted provider switch (PROVIDER=opensource in
# .env.development) — talks to any OpenAI-compatible endpoint, e.g. GPT-OSS
# served locally via Ollama.
openai>=1.50.0
